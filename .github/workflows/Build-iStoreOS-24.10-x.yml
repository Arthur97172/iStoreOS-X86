name: 📦 Build iStoreOS_x86_64 24.10.x

on:
  workflow_dispatch:
    inputs:
      build_version:
        description: "请选择构建版本 (latest 为官网最新)"
        required: true
        default: 'latest'
        type: choice
        options:
          - 'latest'
          - '20251205'
      luci_version:
        description: "选择luci版本"
        required: true
        default: "24.10.4"
        type: choice
        options:
          - '24.10.0'
          - '24.10.1'
          - '24.10.2'
          - '24.10.3'
          - '24.10.4' 
      ipaddr:
        description: '请输入管理IP'
        required: true
        default: '192.168.5.1'
      include_docker:
        description: |
          是否编译 Docker 插件
        required: true
        default: 'yes'
        type: choice
        options:
          - 'yes'
          - 'no'

env:
  VERSION: ${{ inputs.luci_version }}

jobs:
  build:
    runs-on: ubuntu-22.04
    # 关键：添加写入权限，解决 403 报错
    permissions:
      contents: write
    steps:
    
    - name: Checkout code
      uses: actions/checkout@v3

    - name: ⚙️ 设置可执行权限
      run: | 
        chmod +x x86/build_config.sh
        chmod +x shell/*.sh
             
    - name: 🛠️ 安装依赖
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential libncurses5-dev libncursesw5-dev zstd curl unzip tree qemu-utils

    - name: ⚙️ 设置IP、网关和版本号
      run: |
        CONFIG_PATH="./files/uci-defaults/99-custom.sh"
        echo "当前网络配置路径: $CONFIG_PATH"
        
        # 1. 替换版本号占位符
        sed -i "s/版本号/${{ env.VERSION }}/g" "$CONFIG_PATH"
        [ -f "./files/etc/banner" ] && sed -i "s/版本号/${{ env.VERSION }}/g" "./files/etc/banner"

        # 2. 网络逻辑配置 (关键修改点：不再删除行，改用关键词替换)
        sed -i "s/__IPADDR__/${{ inputs.ipaddr }}/g" "$CONFIG_PATH"

        # 3. 替换脚本末尾的描述信息
        sed -i "s/VERXXXX/${{ env.VERSION }}/g" "$CONFIG_PATH"
        
        echo "✅ 配置修改完成，输出预览："
        cat "$CONFIG_PATH"

    - name: ⏬ 下载 ImageBuilder
      run: |
        BUILD_VER="${{ inputs.build_version }}"
        if [ "$BUILD_VER" = "latest" ]; then
          URL="https://fw0.koolcenter.com/iStoreOS/ib/x86_64/istoreos-imagebuilder-x86-64.Linux-x86_64.tar.zst"
        else
          URL="https://github.com/${{ github.repository }}/releases/download/iStoreOS-Imagebuilder/${BUILD_VER}-istoreos-imagebuilder-x86-64.Linux-x86_64.tar.zst"
        fi
        curl -Lf -o imagebuilder.tar.zst "$URL"
        tar --use-compress-program=unzstd -xvf imagebuilder.tar.zst
        mv istoreos-imagebuilder-* imagebuilder
        
    
    - name: ⏬ 复制必备文件
      run: |
        cp x86/build_config.sh imagebuilder/
        cp x86/Makefile imagebuilder/
        cp x86/repositories.conf imagebuilder/
        cp shell/prepare-packages.sh imagebuilder/
        cp shell/custom-packages.sh imagebuilder/
        mkdir -p imagebuilder/files/etc/uci-defaults
        cp files/uci-defaults/99-custom.sh imagebuilder/files/etc/uci-defaults/
        mkdir -p imagebuilder/files/etc/opkg
        cp -r files/customfeeds/* imagebuilder/files/etc/opkg/ 2>/dev/null || true
        # 验证信息
        ls -l imagebuilder/files/etc/opkg/customfeeds.conf && echo "已拷贝 customfeeds.conf"
        
        echo "打印action目录结构"
        tree -L 1

    - name: ⚙️ 动态获取 Kmod 哈希并更新配置
      run: |
        BUILD_VER="${{ env.VERSION }}"
        BASE_URL="https://downloads.openwrt.org/releases/${BUILD_VER}/targets/x86/64/kmods/"
        
        echo "正在从 $BASE_URL 获取最新的内核哈希..."
        # 抓取包含哈希值的文件夹名称 (例如 6.6.110-1-xxx...)
        KMOD_DIR=$(curl -sL $BASE_URL | grep -oE "[0-9.-]+-[a-f0-9]{32}" | head -n 1)
        
        if [ -n "$KMOD_DIR" ]; then
          FULL_KMOD_URL="${BASE_URL}${KMOD_DIR}"
          echo "✅ 成功匹配 kmods 路径: $FULL_KMOD_URL"
          
          # 1. 替换版本占位符
          sed -i "s|VERSION_PLACEHOLDER|${BUILD_VER}|g" imagebuilder/repositories.conf
          # 2. 替换 Kmod 完整路径
          sed -i "s|https://.*/kmods/KMOD_PLACEHOLDER|$FULL_KMOD_URL|g" imagebuilder/repositories.conf
        else
          echo "❌ 无法获取 Kmod 哈希，请检查官方源是否存在该版本: $BASE_URL"
          exit 1
        fi
        
        echo "--- 检查更新后的 repositories.conf ---"
        cat imagebuilder/repositories.conf | grep "https"

    - name: 🧱 构建iStoreOS固件
      working-directory: ./imagebuilder
      run: |
        echo "打印imagebuilder目录结构"
        tree -L 1
        echo "打印repositories"
        cat repositories.conf
        echo "打印Makefile"
        cat Makefile |grep here
        bash ./build_config.sh

    - name: 🔍 打印imagebuilder目录结构并重命名固件信息
      run: |
        echo "--- 原始文件列表 ---"
        ls -lah imagebuilder/bin/targets/x86/64/
        
        # 进入目标目录
        cd imagebuilder/bin/targets/x86/64/
        
        for f in *squashfs-combined-efi.img.gz; do
          if [ -f "$f" ]; then
            echo "正在重命名 $f 为 ${{ github.event.inputs.luci_version }}-$f"
            mv "$f" "${{ github.event.inputs.luci_version }}-$f"
          fi
        done
        
        echo "--- 改名后文件列表 ---"
        ls -lah .

    - name: 📜 生成路由器后台基本信息
      run: |
          # 1. 先创建基础信息到文件
          cat > ${{ github.workspace }}/router-info.md <<EOF
          ## 📡 路由器后台信息
          
          📌 版本： iStoreOS-x86-64-${{ github.event.inputs.luci_version }}
          
          🧩 集成： 包含了基础系统组件、驱动、网络工具、容器支持、LuCI界面及插件等
          
          👤 用户名： root
          
          🔒 密码： 无
          
          🌐 初始IP： ${{ github.event.inputs.ipaddr }}
          
          EOF

          # 2. 根据判断追加 Docker 状态
          if [ "${{ github.event.inputs.include_docker }}" == "yes" ]; then
            echo "📜 **Docker 状态**: 已集成" >> ${{ github.workspace }}/router-info.md
          else
            echo "📜 **Docker 状态**: 未集成" >> ${{ github.workspace }}/router-info.md
          fi

    - name: 🚀 上传到 Release
      uses: softprops/action-gh-release@v2.2.1
      with:
        tag_name: iStoreOS-${{ github.event.inputs.luci_version }}-x86-64-RELEASE
        # 3. 使用 body_path 读取刚才生成的文件内容
        body_path: ${{ github.workspace }}/router-info.md
        files: |
          imagebuilder/bin/targets/x86/64/*squashfs-combined-efi.img.gz
          imagebuilder/bin/targets/x86/64/sha256sums
        token: ${{ secrets.GITHUB_TOKEN }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
